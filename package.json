{
  "name": "recmap",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "cd frontend && npm install && npm run build && cd .. && cp -r frontend/.next . || xcopy /E /I /Y frontend\\.next .next",
    "start": "npm --prefix frontend start"
  }
}
